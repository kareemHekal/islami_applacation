import 'package:flutter/material.dart';

class AhadisTab extends StatelessWidget {
  const AhadisTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
